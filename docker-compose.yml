version: '3.8'

services:
  whatsapp-bot:
    build: .
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - whatsapp-sessions:/app/sessions
    environment:
      - PORT=${PORT}

volumes:
  whatsapp-sessions: